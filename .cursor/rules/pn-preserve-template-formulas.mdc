---
description: 账单转换 PN/地区表公式必须以当前客户母版为准，禁止引擎硬编码覆盖
globs: profiles/**/*.py,bill_convert/**/*.py,xlsx_*.py,pdf_ingest/**/*.py
alwaysApply: false
---

# PN / 公式：以当前应用母版为准

## 口令（用户常说）

「PN 以母版为准」「不要硬编码覆盖公式」「母版公式是唯一真相」「不要乱改母版」

## 原则

1. **当前客户挂载的 PN/地区表母版 = 公式唯一真相**。引擎不得用某一供应商/默认模板的列映射去整表重写公式。
2. **人数 == 母版槽位**：整张 PN（及相关公式列）**不动公式**；只写数据（汇率数值、发票元数据、源表金额等）。
3. **人数变化需要扩/缩行**：从母版**首人/首行公式复制**，只平移行号与员工引用；禁止发明业务列引用。
4. **已有公式禁止覆盖**；空单元格也不要擅自发明默认公式（除非产品明确要求）。
5. **允许的例外**：LuckySheet 非法语法修正（如 `="- "&+"`）、日期格式强制（防显示成 `46113`）、明确的数据格写入（如 EE Code、有客户编号时的 Client Code）。

## 反例（已在 UAE 踩过）

- PN Labor/Expense/Tax 硬编码覆盖
- Bank Charges 写死 `10+50`
- UAE!B9 姓名文本覆盖公式
- UAE EE!C 改成 PN 引用
- PN 汇率公式覆盖成裸数值

## 现状

- **UAE**：按上述原则收紧中。
- **TW / HK / China**：仍硬编码；一母版且测过则暂不改。
- **UK**：接近保母版；一母版可维持。
